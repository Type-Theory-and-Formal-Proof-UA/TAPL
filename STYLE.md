# Правила перекладу (TAPL → українська)

Читай цей файл повністю перед початком. Він обов’язковий.

Це переклад книги Benjamin C. Pierce, *Types and Programming Languages* (MIT Press, 2001)
українською. Термінологія узгоджена з уже наявним перекладом ATTаPL
(`/Users/ihor/atapl/GLOSSARY.md`) — обидві книги одного автора, і терміни мусять збігатися.

## 1. Що перекладаємо

Повний переклад, не переказ. Кожне означення, доведення, приклад, зауваження, вправа,
підпис рисунка, зноска й посилання переноситься. Якщо в доведенні загубився крок — це
дефект, а не стилістичний вибір. Нічого не скорочуй.

**Що НЕ перекладається (лишається як в оригіналі):**
- назви правил виведення й типізації: `T-App`, `E-IfTrue`, `E-If`, `S-Refl`, `T-Sub`, `CTX-App`;
- назви мов, систем і числень: `B`, `NB`, `Bool`, `Nat`, `ML`, `OCaml`, `Standard ML`,
  `λ→`, `λ!`, `λ<:`, `F<:`, `System F`, `Fω`, `λ_2`, `PCF`, `Featherweight Java`, `FJ`,
  `TAPL`, `ATTaPL`, `arith`, `untyped`, `fullref`, `fullsub`, `purefsub`;
- імена авторів у бібліографії, заголовках і посиланнях: `Pierce`, `Aho`, `Sethi`, `Ullman`,
  `Plotkin`, `Kahn`, `Milner`, `Damas`, `Mycroft`, `Curry`, `Howard`, `Girard`, `Reynolds`;
- ключові слова мов програмування в коді (`let`, `fun`, `if`, `then`, `else`, `type`,
  `in`, `true`, `false`, `unit`, `int`, `bool`, `succ`, `pred`, `iszero`, `ref`, `raise`, `try`);
- літерні позначення метазмінних у формулах: `t`, `v`, `s`, `r`, `T`, `S`, `U`, `R`, `P`, `Γ`, `Σ`, `μ`;
- назви теорем і лем, наведені як імена правил, коли вони цитуються як `T-Sub` тощо.

Усе інше — включно з пояснювальним текстом усередині рисунків і таблиць — перекладай.

## 2. Структура файлу

Файл, який ти пишеш, — це Typst. Він мусить починатися так:

```typst
#import "/templates/preamble.typ": *

#chap("3", "Негіповані арифметичні вирази")
```

Для розділів без номера (передмова, додатки, бібліографія) номер порожній:

```typst
#chap("", "Передмова")
```

Далі — тіло. Заголовки розділів і підрозділів:

- `#sec("3.1", "Вступ")` — розділ;
- `#subsec("Формалізми")` — підрозділ;
- `#subsubsec("Індукція за глибиною:")` — підпідрозділ.

Номер розділу (`3.1`) лишається **латиницею-цифрами як в оригіналі**, назва — перекладена.
`#sec(...)` скидає лічильник тверджень, тому кожен розділ нумерується з одиниці — саме так,
як у книзі. **Розділи мусять іти в тому ж порядку, що й в оригіналі, і жодного середовища
не можна пропустити чи додати — інакше всі номери після пропуску поїдуть.**

## 3. Середовища тверджень

**Номер твердження передається явно, як в оригіналі** — лічильника немає:

```typst
#defn("3.2.1")[Терм $t$ є #emph{значенням}.]
#exr("3.5.1", diff: "★★", rec: true)[Скільки елементів має $S_3$?]
```

Чому явно: у книзі номери подекуди йдуть **не за порядком друку** (у розділі 4 вправа
4.2.2 надрукована перед 4.2.1; те саме в 23.7.2 перед 23.7.1), і лічильник таке зламав би.
Явний номер заодно дає перевірячу змогу звірити послідовність номерів із джерелом.

| Оригінал | Українською |
|---|---|
| Theorem | `#thm("N")[…]` |
| Lemma | `#lem("N")[…]` |
| Corollary | `#cor("N")[…]` |
| Definition | `#defn("N")[…]` |
| Proposition | `#prop_("N")[…]` |
| Claim | `#claim("N")[…]` |
| Notation | `#notation("N")[…]` |
| Convention | `#convention("N")[…]` |
| Fact | `#fact("N")[…]` |
| Axiom | `#axiom("N")[…]` |
| Example | `#example("N")[…]` |
| Remark | `#remark("N")[…]` |
| Exercise | `#exr("N")[…]` |
| Solution (додаток A) | `#solution("N")[…]` |
| Figure | `#figure([Рисунок 3-1: …], body)` |

`N` — **точний номер із джерела** (у джерелі він стоїть у рядку `<<<STMT 3.2.1>>>` перед
текстом). Не вигадуй номер і не перенумеровуй.

`exr` приймає: `diff` — рядок зірочок з оригіналу (`"★"`, `"★★"`, `"★★★"`, `"★★★★"`,
для вправ зі стрілкою — `"★ ↛"`), `rec: true` — позначка `[Recommended]`.
Порядок у дужках оригіналу саме такий: спершу `Recommended`, потім зірочки.
Приклад зі шкали (передмова, с. xii): ★ Quick check, ★★ Easy, ★★★ Moderate, ★★★★ Challenging.
`↛` означає, що розв’язку в додатку A немає — цю позначку зберігай.

Якщо в оригіналі твердження **не має** номера (просте речення, не виділене середовищем),
не загортай його в середовище. Якщо середовище цитує чуже твердження з оригінальним
номером — надрукуй номер текстом, а не середовищем.

**Додаток A** — це самі лише `Solution:` для вправ. Кожен розв’язок — `#solution("N")[…]`
з тим самим номером, що й вправа в основному тексті.

**Увага до порядку надрукованих номерів.** Якщо в джерелі номер іде не за зростанням —
це не помилка джерела, а справжній порядок книги; переноси його як є.

## 4. Математика

Математика в Typst — у доларах: `$Gamma tack t : T$`. Текст усередині математики
пишеться в лапках: `$T ::= "Bool" mid T times T$`.

**Перевірена таблиця символів** (перевірена компіляцією та читанням готового PDF —
користуйся тільки цими написаннями):

| Потрібен символ | Пиши |
|---|---|
| ⊢ | `tack` |
| → | `arrow.r` |
| ⇒ | `arrow.r.double` |
| ⇔ | `arrow.l.r.double` |
| ⟺ | `arrow.l.r.double.long` |
| ↦ | `arrow.r.bar` |
| ⟶ (довга) | `arrow.r.long` |
| ↑ / ↓ | `arrow.t` / `arrow.b` |
| ⪯ | `prec.eq` |
| ≼ | `prec.curly.eq` |
| ⊑ | `subset.eq.sq` |
| ⊆ / ⊇ | `subset.eq` / `supset.eq` |
| ∣ (ділить) | `divides` |
| ∈ / ∉ | `in` / `in.not` |
| ∅ | `emptyset` |
| ∀ / ∃ | `forall` / `exists` |
| ∧ / ∨ / ¬ | `and` / `or` / `not` |
| ≠ / ≤ / ≥ | `eq.not` / `lt.eq` / `gt.eq` |
| ⊥ / ⊤ | `bot` / `top` |
| ℓ | `ell` |
| ∂ | `partial` |
| ∇ | `nabla` |
| ≔ | `colon.eq` |
| ≡ | `equiv` |
| ≈ | `approx` |
| ∼ / ≃ | `tilde` / `tilde.eq` |
| ∘ | `compose` |
| ⋅ | `dot.op` |
| ⋆ | `star` |
| ∥ | `parallel` |
| ⊧ / ⊩ | `models` / `forces` |
| ▶ | `triangle.r` |
| □ (кінець доведення) | `square` |
| ★ (складність) | `star.filled` |
| ↛ | `arrow.r.not` |
| ∣ у граматиці (розділювач альтернатив) | `divides` |
| ▶ | `triangle.r.filled` |
| ⟨ ⟩ | `chevron.l` `chevron.r` |
| ∞ | `infinity` |
| × | `times` |
| λ, Γ, Δ, Π, Σ, Ψ, Λ, Ω | `lambda`, `Gamma`, `Delta`, `Pi`, `Sigma`, `Psi`, `Lambda`, `Omega` |
| α β γ δ ϵ ε ζ η θ κ μ ν ξ ρ σ τ φ ϕ χ ψ ω | `alpha` `beta` `gamma` `delta` `epsilon.alt` `epsilon` `zeta` `eta` `theta` `kappa` `mu` `nu` `xi` `rho` `sigma` `tau` `phi` `phi.alt` `chi` `psi` `omega` |

**Увага — пастки, на яких уже горіли (усе перевірено компіляцією):**
1. `arrow.r.double.long` друкує **⟹**, а не ⇒. Для ⇒ завжди `arrow.r.double`.
2. `arrow.r.long.double` друкує те саме ⟹. Не користуйся ним.
3. Для довгої одинарної стрілки ⟶ пиши `arrow.r.long`.
4. **`mid` — це не символ, а слово-оператор**, і в математиці друкується як ∣ **лише коли
   стоїть між двома операндами**: `$T ::= "Bool" mid T times T$` працює, а
   `$t ::= "true" mid "false"$` друкує літеральне «mid». Тому **для граматик завжди**
   користуйся `divides`: `$t ::= "true" divides "false"$`. Те саме з `tack`, `in`, `and`:
   якщо символ друкується словом — заміни на `divides` або на сам символ Unicode.
5. `star` друкує **⋆** (п’ятипроменева зірка), а `star.filled` — **★**. Для складності
   вправ потрібен саме `star.filled`.
6. `triangle.r` друкує **▷**, а `triangle.r.filled` — **▶**.
7. Немає символів `otimes`, `oplus`, `angle.l`, `angle.r`, `angle.up`, `angle.down`,
   `times.circle`, `preceq`, `turnstile`. Якщо вони трапляються в оригіналі — вставляй сам
   символ Unicode у математику: `$⊗$`, `$⋄$`, `$▶$`, `$⪯$`. Це перевірено.
8. **Алгоритмічний знак виведення** в розділі 16 книга друкує як ⊢, за яким іде ▶
   (U+25B6). Символа `tack.rightharpoon` **не існує** — `typst` видає `unknown symbol
   modifier`. Пиши два символи: `$tack ▶$`, тобто літеральний `▶` після `tack`.

9. **Символа `larrow` не існує.** Оновлення поля запису (розділ 32) пиши літеральною
   стрілкою Unicode: `$r ← x = 5$`.

10. **Усередині `$…$` знак `#` входить у код-режим Typst** і дає `unknown variable`.
    Мітку інваріантного поля (розділ 32) пиши ідентифікатором: `${hash x : "Nat"}`,
    `$r ← hash x = 5$`; поза математикою — звичайний `#` у `#raw("...")`.

11. Так само **не існують** `lArr`, `rArr`, `sect`, `arrow.up.double`. Для них пиши
    літерали Unicode: `$⇐$`, `$⇒$`, `$∩$`, `$⇑$` (розділ 28).

12. **Напівжирний — це одна зірочка:** `*текст*`. Подвійні `**текст**` не працюють —
    Typst надрукує зірочки буквально (і дасть лише попередження, не помилку).

13. **Усередині `$…$` не можна вкладати лапки.** `$"("Nat" -> "Nat") -> …$` дає
    `unclosed delimiter`. Зовнішні лапки навколо всього виразу зайві — пиши
    `$("Nat" -> "Nat") -> ("Nat" -> "Nat")$`. Кожен багатолітерний ідентифікатор
    усередині математики береться в *свої* лапки окремо.

14. **Кома ПІСЛЯ `}` чи `$` усередині `$…$` ламає парсер** (`unclosed delimiter`).
    `$S = T_1 -> T_2, і …$` — помилка, бо `,` читається як роздільник. Пиши кому
    *поза* математикою: `$S = T_1 -> T_2$, і …`. Те саме з `$t = x, $Gamma…$` —
    кожен математичний відрізок закривай ПЕРЕД комою.

15. **`#eqn(body, num)` вимагає номер.** Для виключної формули без номера вживай
    `#disp($…$)`. Обидва хелпери самі обгортають тіло в математику, тож передавай
    `$…$`, а не сирий вираз.

16. **`cases(...)` у математиці не має вирівнювання `&`** (це код-оператор). Пиши
    рядки через `quad` як роздільник і не вживай `&`.

17. Символів `subseteq`, `sect`, `gc` (як індексу) не існує. Пиши `subset.eq`,
    `inter`, а багатолітерні індекси берi в лапки: `$->_\("gc"\)$`.

**Граматики TAPL** пиши саме так (перевірено, друкує ∣, а не «mid»):

```typst
$t ::= "true" divides "false" divides "if" t "then" t "else" t$
$v ::= "true" divides "false"$
$T ::= "Bool" divides "Nat" divides T -> T$
```

Індекси: `$T_1$`, `$x_i$`, `$Gamma_1, Gamma_2$`. Пробіли в математиці значущі: пиши
`$Gamma tack t : T$`, а не `$Gammatackt:T$`.

Конструкції, які в TAPL трапляються постійно:

```typst
$t ::= "true" mid "false" mid "if" t "then" t "else" t$   // граматика
$t_1 -> t_1'$                                             // крок обчислення
$Gamma tack t : T$                                        // судження типізації
$mu X. T$                                                 // рекурсивний тип
$forall X. T$                                             // універсальна квантифікація
${ x : T }$                                               // запис
$"fix" f = lambda x. f (x x)$                             // рекурсія через нерухому точку
```

## 5. Рисунки, правила виведення, таблиці

Оригінал містить багато рисунків із правилами виведення та боксами синтаксису. Вони
переносяться так:

```typst
#figure([Рисунок 3-1: Логічні вирази (B)], rules([
  Syntax   $t ::= "true" divides "false" divides "if" t "then" t "else" t$
  #rule("", "E-If", $"if" t_1 "then" t_2 "else" t_3 -> "if" t_1' "then" t_2 "else" t_3$)
]))
```

`rules(body: …)` малює рамку й зберігає розбиття на рядки, як надруковано.
`rule(посилки, назва, висновок)` малює посилки над рискою з назвою праворуч і висновок
під нею. Назву правила (`E-If`) подавай рядком у лапках.

Усередині `rules[…]` не можна користуватися `#h(...)`, `#v(...)`, `#grid(...)`; текст
просто пиши рядками, а `#rule(...)` став окремим рядком.

**Розриви рядків робить preamble — не додавай `#set raw(block: true)` у свою частину.**
І `#code[…]`, і `rules[…]` самі вставляють розрив між двома сусідніми `#raw(...)`
(хелпер `_lines`), бо послідовні `#raw` інакше зливаються в один абзац. Пиши один
`#raw(...)` на рядок джерела — і все. Локальний `#set raw(block: true)` — зайва
копія цієї логіки, яку потім доводиться прибирати вручну (так було в ch13).

Якщо з верстки незрозуміло, як розкласти рисунок, відтвори зміст рядок за рядком — важливо
не загубити жодного правила. **Не вигадуй геометрію й не витрачай виклики на вимірювання
координат: розбиття на рядки вже є в тексті джерела** (кожен рядок рисунка в джерелі
починається з чотирьох пробілів усередині `<<<FIGURE-BODY>>>`).

Підпис рисунка перекладай, але номер лишай як в оригіналі: `Figure 3-1:` → `Рисунок 3-1:`.

**Позначки зірочок у складності вправ** — це `$star$`, а не літеральний `★`, щоб шрифт
друкував однаково. `↛` — `$arrow.r.not$`.

## 6. Мова

- Апостроф — **тільки `’` (U+2019)**: об’єкт, зв’язування, підстановка, ім’я, комп’ютер.
  Не `'` і не `ʼ`.
- Терміни — зі `GLOSSARY.md`, без варіантів. Термін, якого там немає, бери найуживаніший
  і **повідом у звіті** (поле `new_terms`), а не вигадуй свій.
- Лапки: «…» для цитат і назв, “…” лише якщо так у оригіналі всередині.
- Тире: — (U+2014) у тексті.
- Позначка ділення ∣ у математиці — `divides` (а не `mid`, який друкує слово).
- Курсив — `#emph[…]` із **квадратними** дужками; `#emph{…}` надрукує `emph{…}` буквально.
  Так само `#strong[…]`, а не `#strong{…}`.
- Не калькуй російські звороти: не «являється» (правильно «є»), не «відповідно до» замість
  «згідно з», не «на протязі» (правильно «протягом»), не «слідуючий» (правильно «наступний»),
  не «існуючий» у значенні «наявний». Пиши живою фаховою українською, як пишуть підручники
  з теоретичної інформатики.
- Англіцизми на кшталт «інстанціювання» (правильно «примірник») — уникай кальок, де є
  нормальний український відповідник.
- Слова «лема», «теорема», «аксіома» — нормальні українські терміни, вживай їх.

## 7. Що заборонено

- Редагувати `templates/preamble.typ`, `GLOSSARY.md`, `manifest.json` або файли інших
  частин. Їх має лише батьківський агент. Нові терміни — у звіті, не в глосарії.
- Писати в теку перекладу щось, крім свого файлу `uk.typ`. Пробні файли — у
  `/Users/ihor/.hermes/cache/scratch`.
- Лишати англійський текст у прозі (крім переліченого в п.1).
- Лишати маркер `TODO`, коли текст уже перекладено. І **прибирати** `TODO`, коли розділ
  справді перекладено: наявність `TODO` — це сигнал, що робота не завершена.
- Скорочувати, переказувати, «залишати на потім».

## 8. Обов’язкова перевірка перед звітом

```bash
cd /Users/ihor/TAPL
typst compile --root . out/<частина>/uk.typ /Users/ihor/.hermes/cache/scratch/<частина>.pdf
```

Далі **прочитай готовий PDF**, а не лише код виходу:

```bash
pdftotext -layout /Users/ihor/.hermes/cache/scratch/<частина>.pdf - | head -60
```

Переконайся, що символи друкуються правильно (⇒, а не ⟹; ⪯; ⊢; ★; ↛), що кирилиця на місці
й що немає порожніх боксів. Файл, який не компілюється, вважається неперекладеним.

**Головне: перевіряй рендер, а не джерело.** Будь-яка перевірка, що читає `.typ` замість
PDF, не бачить, коли Typst викидає вміст за межі сторінки. Запусти:

```bash
python3 scripts/46_fidelity.py <частина>
```

Він перекомпільовує частину й звіряє, що кожен рядок `#raw("...")` справді є в тексті PDF.
Саме так було знайдено втрату останніх 14 рядків рисунка 26-1: тіло фігури не можна
загортати в `block(breakable: false)`, бо довга фігура не переноситься на наступну
сторінку, а малюється ЗА нею (це вже виправлено центрально в `templates/preamble.typ`).

Не міряй вірність **лічбою символів**: дошки правил багатоколонкові, і видобувач тексту
переставляє частини рядка, тож рядок із цілком правильним гліфом може виглядати «коротшим».
Вірна ознака справжньої втрати — зникає СУЦІЛЬНИЙ відрізок рядків, тобто пропадають усі
його токени.

Що стосується стрілок, розрізняй їх за регістром операндів: `t ⟶ t′` (зведення) друкується
ДОВГОЮ стрілкою `$arrow.r.long$`, а `T → T` (тип функції) — короткою `$arrow.r$`. Тому `->`
з маленькими операндами в математиці — це майже завжди зведення, і його треба замінити.

Пиши файл **у міру готовності**: переклав розділ — зберіг — скомпілював. Не накопичуй усі
правки на один фінальний запис: якщо сесія обірветься, незаписане втрачено повністю.

## 9. Звіт

Поверни JSON:

```json
{
  "file": "out/ch03p01/uk.typ",
  "sections_translated": ["3.1 Вступ", "3.2 Синтаксис"],
  "compiled": true,
  "words": 8323,
  "new_terms": [{"en": "untyped", "uk": "негіпований"}],
  "notes": "що лишилось незрозумілим або потребує рішення"
}
```

`compiled` — тільки якщо `typst compile` справді пройшов успішно на твоєму файлі.
